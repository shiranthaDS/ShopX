name: Trigger auto deployment for inventory-service

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/inventory-service-AutoDeployTrigger-9b448794-b13c-4e29-b52b-86ac1ac735d2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.INVENTORYSERVICE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.INVENTORYSERVICE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.INVENTORYSERVICE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/backend/inventory-service
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: shopx.azurecr.io
          registryUsername: ${{ secrets.INVENTORYSERVICE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.INVENTORYSERVICE_REGISTRY_PASSWORD }}
          containerAppName: inventory-service
          resourceGroup: shopx-u
          imageToBuild: shopx.azurecr.io/inventory-service:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


